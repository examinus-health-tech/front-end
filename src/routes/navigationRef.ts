import { createNavigationContainerRef } from '@react-navigation/native';
import { AppNavigatorRoutesProps } from './app.routes';

export const navigationRef = createNavigationContainerRef<AppNavigatorRoutesProps>();
